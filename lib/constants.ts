/**
 * Values that are true everywhere in Hertz Lab.
 *
 * Hertz Lab sells to Brazil in BRL and does not intend to do otherwise. These
 * constants are not an i18n seam — they exist so the locale and currency are
 * stated once instead of scattered as string literals across the formatters.
 */

export const LOCALE = "pt-BR";

export const CURRENCY = "BRL";
